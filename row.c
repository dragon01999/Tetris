#include <stdio.h>
#include "tetris.h"
#include "render.h"

int main() {
	for (int i = 0; i < WIDTH/2; i+=2)
		game_board[19][i]
	if (isrowTrue(19))
		printf("True");
	else
		printf("false");
	return 0;
}
